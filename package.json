{
  "name": "yama-niyama",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Ten moral principles of Yama & Niyama — each principle its own standalone page. A Guide to Human Conduct.",
  "scripts": {
    "generate": "node scripts/generate.mjs",
    "predev": "node scripts/generate.mjs",
    "dev": "vite",
    "prebuild": "node scripts/generate.mjs",
    "build": "vite build",
    "postbuild": "node scripts/flatten.mjs",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^6.0.0"
  },
  "dependencies": {
    "gsap": "^3.15.0"
  }
}
